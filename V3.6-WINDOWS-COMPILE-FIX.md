# V3.6 Windows compile fix

The first V3.6 Windows/MSVC checkpoint reached the generated `components/sceneutil/shadow.cpp` compile and exposed a missing declaration for the `Settings::cells()` accessor.

The V3.6 shadow-culling generator now adds `components/settings/values.hpp` after the GPU-profiler layer introduces the Cells settings dependency. This preserves the generated-source layering and fixes the accessor declaration without committing generated C++ directly.

A source-only preflight on commit `75a18c24d546a73743f8684f1047f06efce56ac2` passed before this rebuild checkpoint.
