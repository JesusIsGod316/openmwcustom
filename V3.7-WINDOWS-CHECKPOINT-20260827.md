# V3.7 Windows checkpoint

Source preflight run #110 passed at `2c068a93996475e2093fda368de224b5ad347a4d` after validating the complete generated V3.7 source stack, updated safety markers, and the PowerShell launcher parse.

The first Windows attempt failed before configuration because the reusable Windows workflow still invoked the legacy `apply_diagnostic_harness.py` entrypoint. V3.7's validated entrypoint is `apply_diagnostic_harness_v37.py`, which replaces the legacy V3.7 tail with the complete ordered V3.7 packaging stack. The Windows workflow now selects that entrypoint only for V3.7 branches and preserves the legacy entrypoint for older V3 branches.

Source preflight run #113 passed at `e67669e32b07e62e990212c71bac75811de9f4cd`, including generated-source validation and PowerShell launcher parsing. The Windows workflow was restored byte-for-byte to its prior behavior except for the six-line V3.7 entrypoint selection.

This checkpoint requests the next intentional Windows/MSVC build for the current V3.7 batch. The batch includes the proven far-caster-pruning profile promotion plus the default-off/controlled V3.7 hitch, companion-keyframe, resource-sweep, adapter-aware speculative-preload admission, and far-cascade texel-stabilization paths.

No stale whole-far-cascade reuse is promoted by this checkpoint, and no destructive VRAM eviction policy is enabled.
