V3.21 CP4 — SHADOW AND ANIMATION COMPATIBILITY SOURCE AUDIT
============================================================

FOUNDATION
- Branch: v3.21-cp4-shadow-compat
- Parent: e9209a0bc5e06af3b463af2c6cb6f88fd512e04f
- Mode130: accepted CP3 owner-view control, unchanged
- Mode131: exact Mode130 plus CP4 shadow compatibility

SHADOW ARCHITECTURE
1. Keep the real animated head, hair and helmet parts on the existing player
   skeleton; do not create a second actor or duplicate animation pipeline.
2. Mark only those owner-hidden parts with Mask_PlayerSecondaryView.
3. The primary scene camera and gameplay intersection visitor exclude that mask.
4. Player-shadow traversal includes the mask only when player shadows are on.
5. Water reflection and refraction traversals include the mask when they include
   the player, preserving a complete equipped secondary-view silhouette.
6. Mode130 retains CP3's complete removal path for causal comparison.
7. Normal third person never receives the secondary-view mask.

COMPATIBILITY CONTRACT
1. camera.getMode() remains MODE.FirstPerson in full-body first person.
2. camera.isFullBodyFirstPerson() remains the exact feature-state query.
3. camera.getAnimationMode() returns MODE.ThirdPerson in full-body first person
   and otherwise matches getMode().
4. Lua API documentation describes all three semantics so animation replacement
   mods can use their existing third-person branch without a custom mode enum.
5. Older OpenMW compatibility remains possible with a feature-presence fallback.

VALIDATION
- Local complete generated-source preflight: PASS (2026-09-02).
- Local YAML, Python, diff-check and CP4 static identity gates: PASS.
- Remote full-build publication trigger: PUSHED (2026-09-02).
- Build identity must name CP4 and the exact branch.
- Mode130 must not enable OPENMW_V321_CP4_SHADOW_COMPAT.
- Mode131 must enable CP2 fairness, CP3 full body and CP4 shadow compatibility.
- Owner view must remain identical to accepted CP3: no face, hair or helmet.
- Sunlit player shadow should include the head and equipped helmet/hair profile.
- First/third/first switching must restore ordinary third-person rendering.
- Lua QA: getMode()==FirstPerson, isFullBodyFirstPerson()==true, and
  getAnimationMode()==ThirdPerson in Mode131.
