# V3.3 Lua-tail / far-cascade recovery checkpoint

Recovered from the interrupted local Codex worktree on 2026-08-26 and reconstructed as authored generator sources only.

Base diagnostic checkpoint: `bc2695b75b33448d293cb5a9cf80256f10676344`.

This checkpoint adds two independent, default-off experiments:

- `[Lua] v3.3 idle timer fast path = false`
  - already-loaded containers with no due simulation/game timer may skip the otherwise empty protected Lua call;
  - due timers, restored timers, timer ordering, worker synchronization, and catch-up semantics remain unchanged.
- `[Shadows] v3.3 far cascade resolution divisor = 1`
  - divisor 2 renders only the farthest cascade at half width/height;
  - near/middle cascades remain full resolution;
  - actor, player, terrain, and object caster configuration is preserved.

It also adds thresholded `OPENMW_V33_LUA_CALLBACK_FILE` attribution for slow Lua phases and launcher options 12/13/14 for Lua-only, shadow-only, and combined tests.

Generated engine sources are intentionally not committed. `tools/v3/apply_diagnostic_harness.py` applies the V3/V3.2/V3.3 stack and then the new `apply_v33_tail_gpu_experiments.py` pass before packaging/preflight.
