# OpenMW Custom Build

This repository is part of the long-running OpenMW Custom Build project.

Before substantive implementation, establish project context from the authoritative Shared Project Context Archive. Read AI_CONTEXT_CONTROL_BLOCK first, then relevant historical EVENT/legacy records.

Authorities:
- Context Archive: architecture, history, decisions, constraints, current state.
- Benchmark & Log Ledger: exact metrics and validation evidence.
- This Git repository: authoritative source/build state.

Respect promoted foundations, DECISION_LOCKS, hardware/VRAM constraints, benchmark rules, and CURRENT_SNAPSHOT.

Do not repeat rejected approaches unless changed conditions satisfy a documented revisit condition.

Prefer substantive, independently switchable mechanisms over telemetry-only changes.

Before editing:
1. verify branch/commit;
2. inspect relevant existing implementation;
3. compare the proposed mechanism against historical attempts;
4. preserve a control path.

After editing:
- run appropriate source/tests/build checks;
- report exact files changed;
- report compiler/test results;
- report commit/build state;
- distinguish implementation success from runtime performance validation.

Do not invent benchmark conclusions. Runtime promotion/rejection occurs only after user testing and analysis.